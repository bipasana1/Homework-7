<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title>Users</title>
    </head>
    <body>

        <div>
            <h3>users</h3>
        {% for key, value in posts %}
                id : {{ value.id }}
                title : {{value.title}} {# Typo here, should be value.title #}
                views : {{ value.views }}
                <br>
        {% endfor %}

        </div>


    </body>
</html>